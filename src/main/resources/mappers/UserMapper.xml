<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sign.restapi.mapper.UserMapper">
    <select id="userList" resultType="com.sign.restapi.domain.UserDto" parameterType="com.sign.restapi.domain.SearchDto">
        select * from user
        where 1=1
    </select>

</mapper>